import type {FC} from "react";
import type {ICart} from "../../models/ICart.ts";

type Props ={

    cart:ICart
}

export const CartComponent:FC<Props> = () => {
    return (
        <div>
            111
        </div>
    );
};